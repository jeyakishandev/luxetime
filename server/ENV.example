# =============================================================================
# Configuration Base de Données PostgreSQL
# =============================================================================
# Remplacez 'username', 'password' et 'luxetime' par vos propres informations
DATABASE_URL="postgresql://username:password@localhost:5432/luxetime?schema=public"

# Exemple avec les identifiants par défaut de PostgreSQL :
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/luxetime?schema=public"


# =============================================================================
# JWT (JSON Web Token)
# =============================================================================
# Clé secrète pour signer les tokens JWT
# IMPORTANT : Générez une clé aléatoire forte et unique pour la production
# Vous pouvez utiliser : node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=votre_secret_jwt_super_securise_ici_changez_moi_en_production


# =============================================================================
# Configuration Stripe (Optionnel - pour les paiements)
# =============================================================================
# Obtenez vos clés sur https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_votre_cle_stripe_secrete
STRIPE_PUBLISHABLE_KEY=pk_test_votre_cle_stripe_publique


# =============================================================================
# Configuration Email (Optionnel - pour les notifications)
# =============================================================================
# Pour un site portfolio, les emails sont simulés en développement
# En production, configurez un service email (Gmail, SendGrid, AWS SES, etc.)

# URL du frontend (pour les liens dans les emails)
FRONTEND_URL=http://localhost:3000

# Configuration SMTP (optionnel en développement)
# Pour Gmail : utiliser un "App Password" - https://support.google.com/accounts/answer/185833
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_SECURE=false
# EMAIL_USER=votre_email@gmail.com
# EMAIL_PASSWORD=votre_app_password
# EMAIL_FROM="Luxetime" <noreply@luxetime.fr>

# Note : En mode développement sans config, les emails sont simulés (logs dans la console)


# =============================================================================
# Configuration Serveur
# =============================================================================
# Port sur lequel le serveur va écouter
PORT=5000

# Environnement de l'application : development, production, test
NODE_ENV=development


# =============================================================================
# Configuration Upload de Fichiers
# =============================================================================
# Chemin où les fichiers uploadés seront stockés
UPLOAD_PATH=./uploads

# Taille maximale des fichiers en octets (5MB = 5242880 bytes)
MAX_FILE_SIZE=5242880


# =============================================================================
# INSTRUCTIONS D'INSTALLATION
# =============================================================================
# 1. Copiez ce fichier et renommez-le en "config.env"
#    cp ENV.example config.env
#
# 2. Modifiez les valeurs ci-dessus avec vos propres configurations
#
# 3. Assurez-vous que PostgreSQL est installé et en cours d'exécution
#
# 4. Créez la base de données :
#    createdb luxetime
#    ou via psql : CREATE DATABASE luxetime;
#
# 5. Initialisez la base de données avec Prisma :
#    npx prisma generate
#    npx prisma db push
#    npm run seed
#
# 6. Démarrez le serveur :
#    npm run dev
#
# =============================================================================

